const validateRegex=()=>{const[e,t,n]=["regex","text","result"].map(e=>document.getElementById(e));try{const o=new RegExp(e.value).test(t.value);n.textContent=o?"El texto coincide con la expresión.":"El texto NO coincide con la expresión.",n.style.color=o?"green":"red"}catch{n.textContent="Expresión regular inválida.",n.style.color="red"}};